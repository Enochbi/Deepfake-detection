# Deepfake CPU Benchmark - Requirements
# Python 3.10 (recommande, cf. environment.yml)
# Fusion des deux listes fournies + ajout de ce qui manquait pour le
# classifieur temporel (scikit-learn / scipy / joblib) et pour le benchmark.

# -------------------------------
# Deep Learning
# -------------------------------
tensorflow==2.19.0
# keras est inclus dans tensorflow (ne PAS installer separement)

# -------------------------------
# Vision par ordinateur
# -------------------------------
opencv-python==4.10.0.84

# -------------------------------
# Traitement d'images & numerique
# -------------------------------
numpy==1.26.4
Pillow==10.4.0

# -------------------------------
# Machine Learning classique (classifieur temporel video-level)
# -------------------------------
scikit-learn==1.3.2
scipy==1.11.4
joblib==1.3.2

# -------------------------------
# Utilitaires
# -------------------------------
tqdm==4.66.4
pandas==2.1.4

# -------------------------------
# Optionnel : uniquement si vous relancez aussi l'app web Flask
# (pas necessaire pour le seul benchmark CPU en ligne de commande)
# -------------------------------
# Flask==3.0.0
# Werkzeug==3.0.1
# flask-cors==4.0.0
# python-dotenv==1.0.1
